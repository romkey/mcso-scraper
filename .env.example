# Names to watch for (comma-separated, format: "LastName" or "LastName,FirstName")
# Examples:
#   WATCH_NAMES=Smith,Johnson,Doe
#   WATCH_NAMES=Smith John,Johnson Jane
WATCH_NAMES=

# Slack webhook URL (optional - if not set, messages go to stdout)
SLACK_WEBHOOK_URL=

# How often to check (in minutes)
POLL_INTERVAL_MINUTES=15

# Path to the data file for remembering seen bookings
DATA_FILE=/app/data/seen_bookings.json
